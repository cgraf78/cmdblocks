#!/usr/bin/env bash
# Run all repo-owned cmdblocks test suites.

set -euo pipefail

test_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"

for suite in "$test_dir"/suites/*-test; do
  "$suite"
done

printf 'cmdblocks-test: ok\n'
